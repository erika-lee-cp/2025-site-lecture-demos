// Selects element by CSS selector
let title = document.querySelector('.menu ul li');
console.log(title);

// Selects by class
let menuDiv = document.querySelector('.menu');
console.log(menuDiv);
// Set the background color to lightblue
//menuDiv.style.backgroundColor="lightblue";

// Selects by id
let espressoItem = document.getElementById('item-1');
console.log(espressoItem);
console.log(espressoItem.textContent)
// Set the text to Espresso ($2.50)
//espressoItem.textContent = "Espresso ($2.50)"
